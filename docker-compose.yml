version: '3.3'

services:
  #frontend
  web-mall-admin:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8700:80"
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 60M
        reservations:
          memory: 60M
